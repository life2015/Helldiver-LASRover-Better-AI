# 玩家标记目标优先：研究与修复记录

## 当前结论：0.7.9修正HUD屏幕标志误判

2026-09-22 23:42，PID21428已运行0.7.8；用户确认在45秒采样中标记并换标。markers-20260922-234257共225帧、rover-policy-20260922-234257共432帧。本地avatar584的目标602/745/739均被捕获；有效记录flags出现0x600、0x1200、0x200、0x1a00。旧代码要求0x800，导致多数记录返回none。

原生HUD代码0x13cfd5d每帧以0xffffe3ff清除0x1c00；0x13d038b在屏幕坐标距离比较后设置0x800，0x13d02e9/0x13d03a3分别设置0x1000/0x400。它们是动态屏幕状态，不能当作标记归属或有效期。下面历史章节对0x800“玩家标志”的推断已撤回。

0.7.9不再要求0x800，仍只遍历有界环形队列、选择最新且未过期的本地玩家记录，并与完整身份核验的原生有效敌人候选相交。提交前/活动请求复核忽略0x1c00的变化，保留其余标志、目标ID、标记者、显示类型、持续时间、计时单调性、根/队列边界和实体身份检查。取消/过期仍通过时间或队列变化失效。没有新增游戏写入字段或原生调用。

178项Lua测试通过。真实采样回放中35帧从0:none变为有效目标；候选valid固定true，结果仅证明解码/筛选修复。证据：build/compat-25327279/marker-flags-replay-0.7.9.json；复现脚本replay_marker_flags_079.py。实际转火尚待新包部署后验证。

2026-09-22，游戏build 25327279；已完成静态追踪和两轮外部只读采样，0.7.6仍没有启用标记优先。

## 已有证据

- 工作区filediver的`datalibrary/spottable_component.go`描述SpottableComponent，含标记类型、标记节点、图标、可标记距离等静态配置。`enum/hudmarkertype.go`区分Enemy、EnemyHeavy、EnemyMassive、EnemyFlyer和物品/地图等。它们不是“本地玩家当前标记”的实时字段。
- 新版已捕获的game.dll只读截片中，SpottedEntity字符串RVA为0x224f378，SpottedPosition为0x224f388。代码0xa49c70/0xa49c79根据条件选择名称后调用0xbfda20，提供区分实体标记与位置标记的追踪入口。名称与引用本身不能证明这是可直接调用的Lua API，也不能证明数据来自本地玩家。
- SpottableComponent字符串RVA为0x22486d8，代码引用0x67d795位于组件容量/数组分配路径。不要将“可标记组件”误认为当前标记状态。
- 现有Rover快照只读取原生候选、身份、位置、攻击状态，尚未读取标记者、标记目标、取消/到期数据。策略一旦获得已核验的目标身份，可在既有有效候选内优先选择；无需扩大写入字段种类。

## 尚需验证

追踪标记实体记录，确认本地玩家归属、目标完整身份、有效期、取消标记及目标死亡；区分队友标记、地面位置、物品和敌人。对同一敌人依次进行未标记、标记、取消、改标其他敌人的只读对照，并测试地面及队友标记。

若实现，建议仅在本地玩家标记的敌人仍是原生有效候选时优先；超出原生候选/射程不能靠标记强制攻击。还需明确“优先照射一次继续铺火”与“标记期间持续集火”的产品行为，后者会改变现有短照射目标。当前没有修改这部分逻辑。

证据保存在本机build/compat-25327279/marking-strings.json、marking-xrefs.json与既有模块截片；截片不进入Git或发行包。初查时尚未闭合运行时读取链；后续实测进展见下节，不宣称标记优先功能已完成。

## 2026-09-22任务内只读验证

PID11356，游戏build 25327279。`research/observe_markers.py`仅以QUERY_INFORMATION | VM_READ打开游戏，校验DLL哈希与定位签名，最多采样60秒，每秒约5次。没有调用游戏原生函数、注入或写入标记数据。两轮证据为markers-20260922-221327.jsonl（225样本，约45秒）和markers-20260922-221542.jsonl（200样本，约40秒），均完成，无读取异常。

管理器指针根game.dll+0x347ce30。原生0x13d0800/0x13d0850、0x13d06b0/0x13d0760和标记处理0xa49960共同使用以下布局：

| 字段 | 偏移/含义 |
| --- | --- |
| 环形列表起止 | manager+8、+12，均小于128；只遍历起点至终点（不含终点） |
| 记录 | manager+16+index*88，固定容量128 |
| 标记类型 | record+0，已见1敌人、0位置；也见5与18，不能把所有非零类型当敌人 |
| 坐标 | record+4/+8/+12，三个float |
| 有效时间 | record+16持续时间、+20已流逝时间；原生要求前者大于后者 |
| 标记者 | record+24，与本地avatar实体ID匹配 |
| 标志 | record+28；原生查询0x13d0760要求0x800位，实测普通玩家敌人/位置标记flags=0x1a00 |
| 目标 | record+32实体ID，地面为0 |

第一轮，本地avatar=734（由本地玩家→实体归属链取得），敌人标记目标依次见766、751、752，owner均734；地面标记type=0、target=0。用户确认执行了标敌人、换敌人、标地面。部分已到期记录仍留在环形队列中，如slot24的elapsed已大于duration=8，不能只按队列成员资格当作有效标记。

第二轮，type=1的敌人标记target=834、owner=734，生命周期内同步从faction实体表取得匹配ID及20字节身份`0c237b28ae3a8a9a420300008f1b4000b9020000`。记录从12.437秒出现，到13.640秒不再遍历到；用户确认执行了标记后取消。时间上支持取消后失效，但本轮未分别隔离目标死亡和主动取消的影响。还观察到type=5记录duration=9999，不能把8秒硬编码为所有标记的有效期。

结论：已能只读取得本地玩家的实体标记ID，并与现有实体身份链连接。实现优先排序有明确数据基础；仍需测试队友标记、死亡/ID复用、取消与自然过期、多个标记并存、复活及换局。接入时还必须与Rover原生有效敌人候选相交，避免友军/物品或射程外目标被误选。现有Mod代码没有接入标记读取或优先策略，读表脚本仅为研究工具。

采样器目前保留原始队列和推测字段供核对，不是可直接用于写入判断的生产快照：接入时应补充前后根/头尾/记录一致性检查，限制读取失败只让标记优先降级，不能因此停掉普通索敌。

## 0.7.7测试版接入

新增src/markers.lua，只在25327279配置及专用机器码校验通过时读取固定128容量的88字节队列；只采用最新有效本地玩家标记，类型1..4、0x800位、计时和原生有效候选身份必须同时满足。读取后和发起写入前复核根、队列头尾、标记者、类型、目标、标志、持续时间、计时与候选身份；读取失败只取消标记优先。

普通0.4秒攻击轮换时，标记可覆盖30米/相邻距离排序，但不能覆盖当前目标排除或Recent。已经锁定标记敌人时仍短照射转走；标记仍有效且Recent到期后可再次优先。1.5秒超时/最长锁定仍选最近其他敌人，不受标记影响。没有原生候选或还没到正常转火时机，不主动强制锁定。现有0.25秒请求若检测到标记取消/失效，会提前恢复；恢复所用实体检查不依赖标记有效期，避免取消导致掩码无法恢复。

HUD增加MOD: MARKED（沿用0.5秒结束提示保留）、Selecting/Last: your marked enemy及原有Next ID。日志新增marked_target、marker_status；旧布局为unsupported_layout，读失败为unavailable，非敌人标记为not_enemy，不在原生有效候选为not_eligible。

173项Lua测试覆盖队友/地面/友军/物品排除、过期残留、环形回绕、读取失败、身份变化、Recent、超时绕过标记、取消前禁止提交及取消后恢复；两种游戏布局回归通过。新源码尚待部署后的真实标记优先验收；此前两轮只读数据不当作0.7.7行为测试。

2026-09-22 22:36，0.7.7 v15 HUD已部署到本机。包SHA-256：`D6A7E7634B369541962D0792F77A76107CEC7AAFE1AADCAAE4F8E9CB7AA6F3C8`。自有patch_3及sidecar读回校验通过，其余14个patch相关文件不变。旧版备份在`build/pre-deploy-0.7.7-20260922-223649/`。新进程标记优先尚待实机验收。

## 0.7.8修正标记显示类型误判

2026-09-22，PID34252，0.7.7用户反馈标记后未出现MOD: MARKED。并行只读采样markers-20260922-224454与rover-policy-20260922-224454确认：本地avatar608的标记732、733、735使用显示类型5，且相同ID出现在激光狗原生有效候选中。0.7.7的类型1..4限制将它们误判为not_enemy。此前静态枚举名和少量样本不足以将5..8等同于不可攻击友军，这是本次缺陷。

0.7.8允许显示类型1..8继续参与候选匹配，仍要求本地标记者、0x800标志、有效计时、目标完整身份及原生敌人候选资格。不接受地面、队员/物品/目标点等其他显示类型；友军若未被原生候选判定可攻击，仍不会获得优先。新增覆盖5..8与原生资格过滤的回归，174项Lua通过。没有扩大游戏索敌范围、内存写入范围或更改超时策略。

0.7.7章节中类型1..4及直接将5视为友军的解释已由此节纠正；真实阵营资格以原生候选过滤为准。普通标记计时也不能统一假定8秒，始终使用记录自身计时与身份状态。

使用实际原始标记队列与时间相近的原生候选做离线回放，3个样本从旧版0:not_enemy变为732:eligible。回放固定候选valid=true，只验证解码/筛选差异，不等同于实际内存写入及转火验收。报告为build/compat-25327279/marker-type-replay-0.7.8.json。

0.7.8六包已构建，本机仍运行/安装0.7.7；用户当前还在任务内，等待正常退出后更新。

## 2026-09-22 23:36 本地更新至0.7.8

重启后的PID32320日志仍显示0.7.7，且本地Rover归档与0.7.7部署记录一致；此前仅打包，未安装修复版。本次在游戏正常退出后部署0.7.8 v15 HUD，归档和两个sidecar读回校验通过，其余14个patch相关文件未改变。当前patch_5属于EAT700Cooldown 0.2.1，已与其部署记录核对。

安装包SHA-256：`71679AC11290F4A57978FC0870CBDBF49A0DA8C92661FB26CA98C54636D1873B`。Rover patch_3 SHA-256：`9CAEFA406F6CDF5D2FFF9C0078143D5D6147F51900443E5CE4F5B6F2CDC9E19A`。旧文件、部署记录和日志备份于`build/pre-deploy-0.7.8-20260922-233621/`。安装完成不等于运行验收，仍需下次启动确认0.7.8日志及实际标记优先行为。
