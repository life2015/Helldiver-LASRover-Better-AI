# 历史：0.7.1-v14启动冲突核对

归档说明（2026-09-19更新）：以下保留最初0.7.1实验的包身份与当时结论，不作为当前安装流程。当前0.7.2六包已纳入一键构建，见[多渠道说明](packaging-channels.md)及[当前验证](validation.md)。文中的“下一步”是当时计划，实机共存仍无新增确认。

玩家反馈管理器显示与Bingus Shared Loader v14冲突，并补充Rover不生效/没有HUD。源码核对证实原0.7.1及独立v14都替换Wwise回调；v14固定模块名单没有Rover。反馈与独立v14资源获胜的结果相符，但未取得玩家实际部署资源和启动日志，不能把加载顺序当作已经现场验证。

已构建`Rover-Fire-Spread-Experimental-0.7.1-v14.zip`，SHA-256：`57EA51DF7342477CE4000A1277451ECDD4B0774C60569A883C188EF9E077FBF3`。仅替换启动桥为完整官方v14再启动Rover；0.7.1 Rover模块资源逐字节保持一致。管理器资源重叠仍然存在，兼容包必须获胜。

对真实发布v14字节码和新桥进行独立进程离线比较：Wwise回调函数保留、模块名单与状态一致、包含Armory Preview Cache、缺失模块不require、单模块失败不阻断后续、日志目录不可写仍加载、Rover入口初始化、update返回值保留、重复桥不重复初始化、已有v14协调器不重启模块。其他游戏模块为模拟依赖；没有在游戏内运行其他Mod或声称实机共存成功。

打包还验证原0.7.1和官方v14输入哈希、Rover资源未变、归档往返及ZIP完整性。现有118项Rover模拟测试沿用0.7.1结果，因本体未修改未重复运行。当前兼容包未安装到本机，没有改变正在调查的0.7.1整机故障基线、默认构建报告或部署记录。

下一步由反馈玩家正常退出游戏后替换旧Rover包，让兼容包赢得启动冲突并重新部署；检查`RoverFireSpread-startup.log`中的`experimental-0.7.1-v14`、`shared_loader_release=v14`及`rover_state`，同时验证其他已安装Mod的功能。若仍失败，收集实际优先级、资源冲突详情和本次日志。
