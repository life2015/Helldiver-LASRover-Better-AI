# Rover Fire Spread

**当前修复版0.7.9：标记优先不再依赖HUD屏幕焦点位。** 0.7.8实机仍可能忽略本地有效标记；0.7.9移除错误的0x800必要条件，并允许请求期间0x1c00屏幕状态位变化。178项Lua测试及真实采样解码回放通过；实际转火效果仍待安装后验证。详见[标记调查](docs/marked-target-investigation.md)。本轮发行v14/v15的HUD与No HUD四包；`python scripts/package.py --channels v14 v15`仅将这四包放入releases，v12中间包保存在build/internal-base。

**自己的敌人标记优先（build 25327279）。** 在下一次普通转火时优先你的有效敌人标记；仍受Recent 1条/1秒约束，正常短照射后转火，超时仍就近解卡。标记读取失败、取消或过期时使用普通排序，旧游戏布局暂不启用标记功能。HUD显示`MOD: MARKED`及目标ID。下文0.7.8条目保留前一版实现背景，不代表0.7.9已完成实机验收。

目标：让激光护卫犬漫游车短暂攻击后转向旁边的其他敌人，以便铺开点燃。0.7.8优先按敌人之间的距离转火；当前目标在漫游车30米外且圈内有可选敌人时，优先转回圈内。最多保留1个Recent、1秒过期；同一目标连续1.5秒未进入同步攻击状态时，仍重新选择距漫游车最近的其他有效目标。正常攻击轮换窗口约0.4秒，不读取燃烧状态，不保证实际命中或点燃。只有一个可用目标时不强制转火。

**0.7.8相邻排序及30米优先规则尚待实机验证。**已有0.3在两组实机场景中观察到持续转火，普通场景17次请求/17次请求期间目标变化，较密集场景44次/42次；这些历史结果不当作新策略的验证。目标间距离不可用时退回按距漫游车排序，两种距离均不可用时交回原生选择并记录原因。

当前0.7.8的[游戏内运行状态面板](docs/hud.md)位于顶部居中，显示相邻目标或30米近处优先的选择依据，保留Locked/Next并标注超时重选；短暂请求结束后，Next会从请求提交起保留2秒并标注`(last)`，不延长实际请求。174项Lua模拟测试、5项部署测试及实际编译入口/归档检查通过。已登记Steam build **24826606**及**25327279**的布局；按模块哈希对选择配置，关键签名和运行时身份检查仍强制执行，HUD使用对应布局的字体根。新版启动与实战仍待验证。这是本地实验项目，尚未选定项目许可证或完成独立公开发布。

0.7.8增加独立的1.5秒同目标占用兜底，攻击/未攻击状态切换不会重置它；到期后请求最近的其他有效目标。换实体、无效观察或已提交请求时重计。已通过状态交替回归测试，盾虫实战效果仍待确认。见[调查记录](docs/hive-guard-lock-investigation.md)。

0.7.8新增游戏build 25327279 / EXE 1.8.45850.0布局，保留旧版布局。见[游戏更新适配记录](docs/game-update-25327279.md)；具体实机验证状态以该页为准。

## 维护文档

默认v12渠道的ZIP名称为`激光狗索敌优化-0.7.8-v12-内置加载器.zip`（HUD）和`激光狗索敌优化-0.7.8-v12-内置加载器-No-HUD.zip`（无HUD）。

0.7.8每次打包同时生成HUD测试版与`-No-HUD.zip`无界面版，索敌逻辑相同，只能安装其中一个。No HUD完全省略界面模块，仍保留文本日志；本地安装用`deploy.py install --variant no-hud`，切换前先退出游戏并卸载当前包。

0.7.8保留0.7.1加入的绿/黄MOD标题行为：在请求恢复完成后保留0.5秒，标注`(last)`；实际干预已经结束，新活动请求立即覆盖提示。Next仍独立保留至请求提交后2秒。

从 [docs 文档索引](docs/README.md)开始，无需阅读开发对话：

- [实现架构与轮换策略](docs/architecture.md)
- [运行时布局、版本和原生分析依据](docs/runtime-layout.md)
- [开发环境、测试与构建](docs/development.md)
- [安装、Steam启动、升级与回退](docs/deployment.md)
- [日志、只读采样和故障定位](docs/debugging.md)
- [验证记录与下一步测试](docs/validation.md)
- [来源、独立仓库及公开维护准备](docs/maintenance.md)

## 实现概览

确认本地玩家→背包→无人机归属后，Mod筛选允许的下一目标，短暂抑制其他候选并推进原生选敌期限。距离可用时选定一个优先目标，否则保留筛选后的候选池供原生评分选择；实际切换与攻击仍由原生执行，随后控制器比较字段和对象身份并恢复自己仍持有的临时修改。

启动阶段会等待尚未就绪的数据并重试；不一致的快照不用于写入。它不修改行为 ID，不直接指定新目标，不调用游戏原生选敌函数。完整流程、恢复限制和参数入口见[架构文档](docs/architecture.md)。

## 构建与测试

一次`package.py`生成v12、v14、v15各自的HUD/No HUD包，共六个；名称保持“激光狗索敌优化”，所有渠道均标注v12/v14/v15及“内置加载器”或“需要额外安装加载器”。选包、固定依赖和加载顺序见[多渠道说明](docs/packaging-channels.md)。完整构建及玩家说明已按2026-09-19现状整理，近期变化见[变更摘要](docs/changelog.md)。

使用Windows x64 Python和本机游戏Lua DLL。**先按[开发文档](docs/development.md)准备同级源码依赖和固定的Shared Loader输入**，再在本目录依次执行：

```powershell
python scripts/test.py
python scripts/build.py
python scripts/package.py
```

每步成功后再继续。`build.py`默认只生成诊断字节码；`package.py`重新编译实验入口并生成可安装ZIP。构建不会安装或启动游戏。游戏目录可通过`HD2_GAME_ROOT`指定。

正常退出游戏后，可用`python scripts/deploy.py install`安装，`uninstall`移除。保留`build/deployment.json`；安装完成后直接从Steam启动。完整说明见[部署文档](docs/deployment.md)和[包内安装说明](INSTALL.txt)。

调试时`observe.py`仅对游戏做外部只读采样；这与已安装实验Mod在游戏进程内修改数据是不同路径。先确认飞船日志与运行计数，再开始战斗测试。

## 来源

Windows API适配器及归档编码来自CowboyBingus/SentryAimRetention；v12/v14启动桥分别包含固定的Bingus Shared Loader对应发布资源，v15包仅包含Rover模块并依赖独立官方加载器。来源版本、许可现状和后续独立化工作见[维护文档](docs/maintenance.md)。游戏模块快照、原始采样、游戏DLL不应混入公开源码。
